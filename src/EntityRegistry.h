#pragma once

//////////////////////////////////////////////////////////////////////////

#include "EntityDefs.h"

//////////////////////////////////////////////////////////////////////////

class EntityRegistry final: public cxx::noncopyable
{
public:
    EntityRegistry();

    Entity* GetEntityPtr(const EntityHandle& entityHandle) const;

    bool Exists(const EntityHandle& entityHandle) const;
    bool ExistsOnMap(const EntityHandle& entityHandle) const;

private:
    
};

//////////////////////////////////////////////////////////////////////////

extern EntityRegistry gEntities;

//////////////////////////////////////////////////////////////////////////